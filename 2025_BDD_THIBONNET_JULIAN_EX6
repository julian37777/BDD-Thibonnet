On peut supprimer une réservation avec la requête suivante :

DELETE FROM reservation
WHERE id_reservation = 'res003';

On peut supprimer toutes les réservations dont la date de rendue est dépassée par rapport à la date d'aujourd'hui avec la requête suivante :

DELETE FROM reservation
WHERE date_fin < CURRENT_DATE;

Requête pour supprimer un étudiant si besoin :

DELETE FROM utilisateur
WHERE id_etudiant = 'etu004';
